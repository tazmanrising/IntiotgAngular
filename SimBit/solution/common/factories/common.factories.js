(function () {
    "use strict";

    angular
        .module("common.factories",   // name of module
        [])   // array that defines the dependencies  ( ngResource contains the $resource service)
} ());
